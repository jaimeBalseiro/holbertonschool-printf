#ifndef _MAIN_H
#define _MAIN_H
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>
/*From here on below, the prototypes*/

Int _printf(const char *format, …);
int _strcmp(char *s1, char *s2);

#endif

